- [Основы синтаксиса языка C++](#основы-синтаксиса-языка-c)
  - [Целочисленный и вещественный тип данных](#целочисленный-и-вещественный-тип-данных)
  - [Логический типы, числовые и символьные литералы](#логический-типы-числовые-и-символьные-литералы)
  - [Переменные и область видимости, синонимы типов, перечислимый тип данных](#переменные-и-область-видимости-синонимы-типов-перечислимый-тип-данных)
  - [Операторы и выражения, побитовые операции](#операторы-и-выражения-побитовые-операции)
  - [Преобразование типов](#преобразование-типов)
  - [Управление выполнением программы, циклы](#управление-выполнением-программы-циклы)
  - [Функции](#функции)
  - [Структуры](#структуры)
  - [Массивы](#массивы)
  - [Ссылки](#ссылки)
  - [Пространства имён](#пространства-имён)
  - [Многофайловые программы и модули](#многофайловые-программы-и-модули)
  - [Модель памяти и представление данных](#модель-памяти-и-представление-данных)
  - [Основы указателей](#основы-указателей)
  - [Адресная арифметика и массивы](#адресная-арифметика-и-массивы)
  - [Время жизни объектов и способы выделения памяти](#время-жизни-объектов-и-способы-выделения-памяти)
  - [Динамическая память](#динамическая-память)
  - [Работа с сырой памятью](#работа-с-сырой-памятью)
  - [Ошибки при работе с памятью](#ошибки-при-работе-с-памятью)
  - [Современные практики управления памятью](#современные-практики-управления-памятью)
- [Стандартная библиотека языка C++](#стандартная-библиотека-языка-c)
  - [std::string и std::string\_view](#stdstring-и-stdstring_view)
  - [std::vector и std::array](#stdvector-и-stdarray)
  - [std::deque](#stddeque)
  - [std::list и std::forward\_list](#stdlist-и-stdforward_list)
  - [Контейнеры map и multimap](#контейнеры-map-и-multimap)
  - [Контейнеры unordered\_map и unordered\_multimap](#контейнеры-unordered_map-и-unordered_multimap)
  - [Контейнеры set и multiset](#контейнеры-set-и-multiset)
  - [Контейнеры unordered\_set и unordered\_multiset](#контейнеры-unordered_set-и-unordered_multiset)
  - [std::function и лямбда-функции](#stdfunction-и-лямбда-функции)
  - [Ввод-вывод](#ввод-вывод)
  - [Итераторы](#итераторы)
  - [Стандартные алгоритмы](#стандартные-алгоритмы)
  - [std::optional](#stdoptional)
  - [Стандартные умные указатели](#стандартные-умные-указатели)
  - [std::filesystem](#stdfilesystem)
  - [std::variant](#stdvariant)
- [Объектно-ориентированное программирование](#объектно-ориентированное-программирование)
  - [Объектно-ориентированная парадигма программирования](#объектно-ориентированная-парадигма-программирования)
  - [Абстракция, инкапсуляция, инварианты](#абстракция-инкапсуляция-инварианты)
  - [Наследование и полиморфизм](#наследование-и-полиморфизм)
  - [Классы, объекты, поля и методы](#классы-объекты-поля-и-методы)
  - [Константность, свойства объектов, указатель this](#константность-свойства-объектов-указатель-this)
  - [Конструкторы и деструкторы](#конструкторы-и-деструкторы)
  - [Копирование и присваивание объектов](#копирование-и-присваивание-объектов)
  - [Семантика перемещения](#семантика-перемещения)
  - [Статические методы и данные](#статические-методы-и-данные)
  - [Проектирование классов](#проектирование-классов)
  - [Отношения между классами](#отношения-между-классами)
  - [Наследование - 1](#наследование---1)
  - [Наследование - 2](#наследование---2)
  - [Наследование и жизненный цикл объектов](#наследование-и-жизненный-цикл-объектов)
  - [Виды наследования](#виды-наследования)
  - [Интерфейсы и абстрактные классы](#интерфейсы-и-абстрактные-классы)
  - [Приведение типов по иерархии наследования](#приведение-типов-по-иерархии-наследования)
  - [Множественное наследование](#множественное-наследование)
  - [Runtime-полиморфизм с std-variant](#runtime-полиморфизм-с-std-variant)
  - [Перегрузка операций - 1](#перегрузка-операций---1)
  - [Перегрузка операций - 2](#перегрузка-операций---2)
  - [Перегрузка операций - 3](#перегрузка-операций---3)
  - [Перегрузка операций - 4](#перегрузка-операций---4)
  - [Обработка исключений - 1](#обработка-исключений---1)
  - [Обработка исключений - 2](#обработка-исключений---2)
  - [Разработка кода, устойчивого к исключениям - 1](#разработка-кода-устойчивого-к-исключениям---1)
  - [Разработка кода, устойчивого к исключениям - 2](#разработка-кода-устойчивого-к-исключениям---2)
  - [Обобщённое программирование - 1](#обобщённое-программирование---1)
  - [Шаблоны классов](#шаблоны-классов)
  - [Шаблоны с переменным числом аргументов](#шаблоны-с-переменным-числом-аргументов)

# Основы синтаксиса языка C++

## Целочисленный и вещественный тип данных

- Какие встроенные целочисленные типы существуют в C++ и какие гарантии по их размерам предоставляет стандарт? ⭐⭐
- Чем отличаются знаковые и беззнаковые целые типы? Какие проблемы могут возникать при их совместном использовании? ⭐⭐⭐
- Почему тип `char` нельзя считать просто «символом»? ⭐⭐
- В каких случаях стоит использовать `size_t` вместо `int`? ⭐⭐
- Для чего предназначены типы `int32_t`, `uint64_t`, `intptr_t`, `int_fast32_t`, `int_least32_t`? ⭐⭐⭐
- Какие риски связаны с переполнением целочисленных типов? ⭐⭐⭐
- Как выполняется деление и вычисление остатка для отрицательных целых чисел? ⭐⭐
- В каких ситуациях оправдано использование беззнаковых типов? ⭐⭐
- Почему для хранения размеров контейнеров стандартная библиотека использует `size_t`? ⭐⭐
- Когда использование типов фиксированной разрядности является обязательным? ⭐⭐
- Чем вещественные типы отличаются от целочисленных? ⭐
- Какие гарантии стандарт предоставляет для типов `float`, `double` и `long double`? ⭐⭐
- Почему большинство десятичных дробей нельзя представить точно в памяти компьютера? ⭐⭐⭐
- Почему сравнение вещественных чисел на точное равенство может быть ошибкой? ⭐⭐⭐
- Что такое NaN и почему выражение `NaN == NaN` ложно? ⭐⭐⭐
- Что такое положительная и отрицательная бесконечность? В каких ситуациях они появляются? ⭐⭐
- Что такое отрицательный ноль и чем он отличается от обычного нуля? ⭐⭐⭐
- Когда имеет смысл использовать `float`, а когда `double`? ⭐⭐

## Логический типы, числовые и символьные литералы

- Как работает неявное преобразование различных типов к `bool`? ⭐⭐
- Что такое короткое замыкание логических операторов и зачем оно нужно? ⭐⭐
- В каких случаях короткое замыкание позволяет избежать ошибок выполнения программы? ⭐⭐⭐
- Чем логические операторы отличаются от побитовых? ⭐⭐
- В каких ситуациях использование операторов `and`, `or`, `not` может быть полезно или вредно? ⭐⭐
- Какие формы записи целочисленных и вещественных литералов поддерживаются в C++? ⭐
- Как определить тип числового литерала по его записи? ⭐⭐
- Для чего используются суффиксы `U`, `L`, `LL`, `F` и `L`? ⭐⭐
- Какие преимущества даёт разделение разрядов символом `'`? ⭐
- Чем опасны восьмеричные литералы? ⭐⭐
- В каких случаях удобно использовать двоичные литералы? ⭐⭐
- Что представляет собой символьный литерал в C++? ⭐⭐
- Почему символьные литералы могут участвовать в арифметических операциях? ⭐⭐
- Для чего используются escape-последовательности? ⭐
- В каких случаях удобны сырые строковые литералы? ⭐⭐
- Как строковые литералы представлены в памяти? ⭐⭐
- Почему попытка изменить строковый литерал приводит к неопределённому поведению? ⭐⭐⭐
- Чем отличается строковый литерал от объекта `std::string`? ⭐⭐

## Переменные и область видимости, синонимы типов, перечислимый тип данных

- Чем отличается объявление переменной от её инициализации? ⭐⭐
- Почему константа должна быть инициализирована при объявлении? ⭐
- Какие преимущества даёт использование `const`? ⭐⭐
- Как работает автоматический вывод типа через `auto`? ⭐⭐
- Когда использование `auto` улучшает код, а когда ухудшает его читаемость? ⭐⭐⭐
- Что такое область видимости переменной? ⭐
- Что такое сокрытие имён (shadowing) и почему его обычно рекомендуют избегать? ⭐⭐
- Чем отличаются локальные и глобальные переменные? ⭐⭐
- Почему глобальные переменные считаются потенциальным источником проблем? ⭐⭐
- Как различается инициализация локальных и глобальных переменных? ⭐⭐
- Что такое синоним типа и чем он отличается от нового типа данных? ⭐⭐
- Какие преимущества даёт использование `using`? ⭐⭐
- Чем `using` отличается от `typedef`? ⭐⭐
- Почему шаблонные синонимы невозможно объявить через `typedef`? ⭐⭐⭐
- В каких случаях введение синонимов типов повышает сопровождаемость кода? ⭐⭐
- Для каких задач удобно использовать перечислимые типы? ⭐
- Какие недостатки имеют обычные `enum`? ⭐⭐
- Какие проблемы решает `enum class`? ⭐⭐
- Почему отсутствие неявного преобразования к целому числу повышает безопасность `enum class`? ⭐⭐⭐
- Когда использование обычного `enum` может быть оправдано? ⭐⭐⭐

## Операторы и выражения, побитовые операции

- Чем выражение отличается от инструкции? ⭐⭐
- Как влияет тип операндов на результат арифметического выражения? ⭐⭐
- Почему результат выражения `8 / 3` отличается от результата `8.0 / 3`? ⭐⭐
- Чем отличаются префиксная и постфиксная формы инкремента и декремента? ⭐⭐
- В каких ситуациях важно понимать приоритет и ассоциативность операторов? ⭐⭐
- Почему использование скобок часто предпочтительнее запоминания таблицы приоритетов? ⭐⭐
- Что такое тернарный оператор и когда его использование оправдано? ⭐⭐
- Почему присваивание является выражением в C++? ⭐⭐⭐
- Какие преимущества дают составные операторы присваивания (`+=`, `*=`, `<<=` и др.)? ⭐⭐
- Чем побитовые операции отличаются от логических? ⭐⭐
- Для каких задач обычно используются побитовые операции? ⭐⭐
- Как работают операторы `&`, `|`, `^` и `~`? ⭐⭐
- Как работают операторы сдвига и какие ограничения связаны с их использованием? ⭐⭐⭐
- Почему битовые маски широко используются в системном программировании? ⭐⭐⭐
- Как можно извлечь несколько упакованных значений из одного целого числа? ⭐⭐⭐

## Преобразование типов

- В каких случаях компилятор выполняет неявное преобразование типов? ⭐⭐
- Какие проблемы могут возникать из-за неявных преобразований? ⭐⭐⭐
- Почему смешивание знаковых и беззнаковых типов считается опасным? ⭐⭐⭐
- Какие недостатки имеет приведение типов в стиле C? ⭐⭐
- Для чего используется `static_cast`? ⭐⭐
- Для чего используется `const_cast` и почему он требует осторожности? ⭐⭐⭐
- Что такое неопределённое поведение при работе с `const_cast`? ⭐⭐⭐
- Для чего предназначен `reinterpret_cast`? ⭐⭐⭐
- Почему `reinterpret_cast` считается самым опасным оператором приведения? ⭐⭐⭐
- Чем отличаются назначения `static_cast`, `const_cast`, `reinterpret_cast` и `dynamic_cast`? ⭐⭐⭐

## Управление выполнением программы, циклы

- Что такое составная инструкция и зачем она нужна? ⭐
- Какие преимущества даёт объявление переменной непосредственно в условии `if`? ⭐⭐
- В каких случаях удобна конструкция `else if`? ⭐
- Чем оператор `switch` отличается от цепочки `if-else`? ⭐⭐
- Что такое сквозное выполнение ветвей `case` и какие риски оно создаёт? ⭐⭐
- Для чего используется атрибут `[[fallthrough]]`? ⭐⭐
- Почему отсутствие фигурных скобок в условиях может приводить к трудноуловимым ошибкам? ⭐⭐
- В каких ситуациях предпочтительнее использовать `while`, `for` и `do-while`? ⭐⭐
- Чем цикл с предусловием отличается от цикла с постусловием? ⭐⭐
- Почему цикл `do-while` гарантированно выполняется хотя бы один раз? ⭐
- Когда использование бесконечного цикла является оправданным решением? ⭐⭐
- Чем range-based `for` отличается от обычного цикла `for`? ⭐⭐
- Какие требования предъявляются к пользовательскому типу, чтобы его можно было использовать в range-based `for`? ⭐⭐⭐
- Как работают инструкции `break` и `continue`? ⭐⭐
- Какие недостатки имеет инструкция `goto`? ⭐⭐
- В каких редких случаях применение `goto` может быть оправдано? ⭐⭐⭐

## Функции

- Какие преимущества даёт разбиение программы на функции? ⭐
- Чем объявление функции отличается от её определения? ⭐⭐
- Что такое сигнатура функции? ⭐⭐
- В чём состоит правило One Definition Rule применительно к функциям? ⭐⭐⭐
- Какие способы передачи параметров существуют в C++? ⭐⭐
- Когда следует передавать аргумент по значению? ⭐⭐
- Когда следует использовать передачу по константной ссылке? ⭐⭐
- Когда оправдана передача по неконстантной ссылке? ⭐⭐
- Что такое выходной параметр и какие недостатки имеет этот подход? ⭐⭐⭐
- Почему функция должна инициализировать выходной параметр во всех ветках выполнения? ⭐⭐⭐
- Какие аргументы нельзя передать в параметр типа `T&`? Почему? ⭐⭐⭐
- Почему временные объекты можно передавать в параметр типа `const T&`, но нельзя в `T&`? ⭐⭐⭐
- Как выбрать между передачей объекта по значению и по константной ссылке? ⭐⭐⭐
- Что такое перегрузка функций и как компилятор выбирает нужную перегрузку? ⭐⭐⭐
- Почему нельзя перегружать функции только по типу возвращаемого значения? ⭐⭐
- Что такое указатель на функцию и чем он отличается от обычного указателя на объект? ⭐⭐
- Какие задачи решаются с помощью указателей на функции? ⭐⭐
- Какие ограничения имеют указатели на функции по сравнению с функциональными объектами и лямбда-выражениями? ⭐⭐⭐
- Почему лямбды без состояния могут преобразовываться в указатели на функции, а лямбды с состоянием — нет? ⭐⭐⭐
- В каких случаях использование `std::function` предпочтительнее указателей на функции? ⭐⭐
- Какие накладные расходы могут возникать при использовании `std::function`? ⭐⭐⭐

## Структуры

- Какие задачи решают структуры? ⭐
- Чем структура отличается от набора отдельных переменных? ⭐⭐
- Какие способы инициализации структур существуют в C++? ⭐⭐
- Как выполняется инициализация вложенных структур? ⭐⭐
- Чем отличается поведение локальной и глобальной переменной-структуры без явной инициализации? ⭐⭐⭐
- Почему использование неинициализированных полей является опасным? ⭐⭐⭐
- Когда структуру следует передавать по значению, а когда по ссылке? ⭐⭐
- Какие преимущества дают значения полей по умолчанию? ⭐⭐
- Как организовать сравнение сложных структур на равенство? ⭐⭐
- Почему структуры являются важным инструментом моделирования предметной области? ⭐⭐

## Массивы

- Как массивы представлены в памяти? ⭐⭐
- Почему индексация массивов начинается с нуля? ⭐⭐
- Какие риски связаны с выходом за границы массива? ⭐⭐⭐
- Чем отличаются глобальные и локальные массивы с точки зрения инициализации? ⭐⭐
- Что происходит при передаче массива в функцию? ⭐⭐⭐
- Почему массив и структура, содержащая массив, ведут себя по-разному при передаче по значению? ⭐⭐⭐
- Как связаны строковые литералы и массивы символов? ⭐⭐
- Для чего нужен символ `'\0'` в C-строках? ⭐⭐
- Что такое многомерный массив и как он располагается в памяти? ⭐⭐⭐
- Какие ограничения обычных массивов привели к появлению контейнеров стандартной библиотеки? ⭐⭐

## Ссылки

- Что такое ссылка и чем она отличается от указателя? ⭐⭐
- Почему ссылка должна быть инициализирована при создании? ⭐⭐
- Можно ли переназначить ссылку на другой объект после её создания? Почему? ⭐⭐
- Что происходит при присваивании значения через ссылку? ⭐⭐
- Какие преимущества дают ссылки по сравнению с передачей параметров по значению? ⭐⭐
- Что происходит при привязке константной ссылки к объекту другого типа? ⭐⭐⭐
- Почему время жизни временного объекта может продлеваться через константную ссылку? ⭐⭐⭐
- Чем отличаются ссылки на существующий объект и ссылки на временный объект? ⭐⭐⭐
- Какие ошибки могут возникнуть при возврате ссылки из функции? ⭐⭐⭐

## Пространства имён

- Какие задачи решают пространства имён? ⭐
- Почему использование `using namespace` в заголовочных файлах считается плохой практикой? ⭐⭐
- Чем отличается `using namespace std;` от `using std::cout;`? ⭐⭐
- Что такое ADL (Argument-Dependent Lookup) и зачем он нужен? ⭐⭐⭐
- Почему вызов функции может успешно компилироваться без явного указания пространства имён? ⭐⭐⭐
- Для чего используются вложенные пространства имён? ⭐⭐
- Что такое безымянное пространство имён? ⭐⭐
- Чем безымянное пространство имён отличается от глобальной области видимости? ⭐⭐
- Почему безымянное пространство имён считается современной заменой `static` для функций и переменных уровня файла? ⭐⭐⭐

## Многофайловые программы и модули

- Зачем крупные программы разделяют на несколько файлов? ⭐⭐
- Чем отличается заголовочный файл от файла реализации? ⭐⭐
- Что такое единица трансляции? ⭐⭐⭐
- Почему определение функции в заголовочном файле может привести к ошибке компоновки? ⭐⭐⭐
- Для чего используется ключевое слово `inline`? ⭐⭐⭐
- Что такое правило ODR и какие проблемы возникают при его нарушении? ⭐⭐⭐
- Какие проблемы традиционной модели `#include` пытаются решить модули C++20? ⭐⭐⭐
- Чем механизм `import` принципиально отличается от `#include`? ⭐⭐⭐
- Какие преимущества дают модули по сравнению с заголовочными файлами? ⭐⭐⭐
- Как модули помогают разделять публичный интерфейс и детали реализации? ⭐⭐⭐


## Модель памяти и представление данных

- Что представляет собой модель памяти языка C++ и как она обеспечивает переносимость программ? ⭐
- Что в C++ называется объектом? Какие характеристики объекта определяются языком? ⭐
- Чем отличаются понятия «объект», «переменная» и «ссылка» в C++? ⭐⭐
- Что такое выравнивание данных (alignment)?
  Почему компилятор может увеличивать размер структуры по сравнению с суммой размеров её полей? ⭐⭐
- Как связаны между собой `sizeof` и `alignof`? Приведите примеры, когда размер и выравнивание типа отличаются. ⭐⭐
- Почему порядок полей в структуре может влиять на её размер и эффективность использования памяти? ⭐⭐
- Что такое endianess? Чем отличаются little-endian и big-endian архитектуры? ⭐
- В каких практических задачах программисту приходится учитывать порядок байтов в памяти? ⭐⭐
- Почему сетевые протоколы используют понятие network byte order? Какие проблемы это решает? ⭐⭐
- Каким образом программа может получить доступ к байтовому представлению объекта в памяти?
  Какие ограничения и риски связаны с таким подходом? ⭐⭐⭐

## Основы указателей

- Что такое указатель? Какие задачи решаются с его помощью в C++? ⭐
- Чем указатель отличается от ссылки? В каких ситуациях предпочтительнее использовать каждый из этих механизмов? ⭐⭐
- Почему неинициализированные указатели опасны? Какие последствия может иметь их использование? ⭐
- Для чего используется оператор взятия адреса `&`? Какие объекты нельзя адресовать напрямую? ⭐⭐
- Что происходит при разыменовании указателя?
  Почему разыменование некоторых указателей приводит к неопределённому поведению? ⭐⭐
- Что такое нулевой указатель (`nullptr`)?
  Какие преимущества он имеет по сравнению с историческим использованием `NULL` или `0`? ⭐⭐
- Почему тип указателя является частью системы типов C++?
  Почему нельзя без явного преобразования присвоить `double*` указателю типа `int*`? ⭐⭐
- От чего зависит размер указателя? Почему размер указателя не зависит от размера объекта, на который он указывает? ⭐
- Почему взятие адреса ссылки возвращает адрес объекта, а не адрес самой ссылки? ⭐⭐
- Что такое косвенный доступ к объекту? Какие преимущества и риски он даёт? ⭐⭐
- Что такое указатель на константу? Какие операции с ним разрешены и запрещены? ⭐
- Чем отличается указатель на константу от константного указателя? ⭐⭐
- Что означает объявление `const int* const p`? ⭐
- Почему указатель на константу может хранить адрес неконстантного объекта? ⭐⭐
- Как правильно читать сложные объявления указателей с несколькими спецификаторами `const`? ⭐⭐
- В каких случаях использование указателей на константу помогает сделать интерфейс класса или функции безопаснее? ⭐⭐

## Адресная арифметика и массивы

- Почему указатели и массивы тесно связаны в языках C и C++? ⭐⭐
- Что означает выражение `arr[i]` с точки зрения адресной арифметики? ⭐⭐
- Почему операция `p + 1` для указателя зависит от типа данных, на который он указывает? ⭐⭐
- Какие операции адресной арифметики разрешены для указателей? ⭐⭐
- При каких условиях можно вычитать один указатель из другого? Что означает результат такого вычитания? ⭐⭐
- Почему сравнение указателей корректно только в определённых случаях? ⭐⭐⭐
- Что такое указатель на элемент, следующий за последним элементом массива? Для чего он используется? ⭐⭐
- Какие ошибки могут возникнуть при выходе за границы массива с помощью адресной арифметики? ⭐⭐
- Почему адресная арифметика считается мощным, но потенциально опасным инструментом? ⭐⭐
- Чем отличается строковый литерал от массива символов? ⭐⭐
- Почему попытка изменить содержимое строкового литерала приводит к неопределённому поведению? ⭐⭐
- Чем отличаются объявления
    `char msg[] = "Hello";`
    и
    `char* p = "Hello";`
    ? ⭐⭐
- В каких случаях массив указателей может быть предпочтительнее двумерного массива символов? ⭐⭐
- Что такое указатель на указатель? Где подобные конструкции используются на практике? ⭐⭐

## Время жизни объектов и способы выделения памяти

- Какие способы выделения памяти под объекты существуют в C++? ⭐
- Чем отличаются статическое, автоматическое и динамическое время жизни объектов? ⭐⭐
- Какие преимущества и недостатки имеет автоматическое выделение памяти по сравнению с динамическим? ⭐⭐
- Почему локальные переменные обычно размещаются в стеке? Какие преимущества это даёт? ⭐⭐
- Что такое кадр стека функции? Какие данные обычно в нём хранятся? ⭐⭐
- Почему рекурсивные вызовы приводят к появлению нескольких кадров стека одной и той же функции? ⭐⭐
- Что такое переполнение стека (stack overflow)? Какие причины могут к нему привести? ⭐⭐
- Почему использование глобальных переменных считается плохой практикой в большинстве случаев? ⭐⭐
- Какие проблемы возникают из-за зависимости программы от глобального состояния? ⭐⭐⭐

## Динамическая память

- В каких ситуациях требуется динамическое выделение памяти? ⭐
- Чем куча (heap) отличается от стека (stack)? ⭐⭐
- Какие действия выполняет оператор `new` при создании объекта? ⭐⭐
- Какие действия выполняет оператор `delete` при удалении объекта? ⭐⭐
- Почему для массивов необходимо использовать `delete[]`, а не обычный `delete`? ⭐⭐
- Что понимается под транзакционностью операций `new` и `new[]`? ⭐⭐⭐
- Что происходит, если конструктор объекта выбрасывает исключение во время выполнения `new`? ⭐⭐⭐
- Что такое исключение `std::bad_alloc` и в каких ситуациях оно возникает? ⭐⭐
- Почему удаление `nullptr` безопасно? ⭐⭐
- Нужно ли присваивать указателю значение `nullptr` после вызова `delete`? В каких случаях это действительно полезно? ⭐⭐
- Какие преимущества дают контейнеры стандартной библиотеки по сравнению с ручным управлением динамической памятью? ⭐⭐

## Работа с сырой памятью

- Чем отличается выделение памяти от создания объекта? ⭐⭐⭐
- Что такое «сырая память» (raw memory)? В каких случаях возникает необходимость работать с ней напрямую? ⭐⭐⭐
- Что такое размещающий оператор `new` (placement new)? Чем он отличается от обычного `new`? ⭐⭐⭐
- Какие обязанности ложатся на программиста при использовании placement new? ⭐⭐⭐
- Почему для placement new важно соблюдать требования к выравниванию памяти? ⭐⭐⭐
- Какие проблемы возникают при попытке удалить объект, созданный placement new, с помощью обычного `delete`? ⭐⭐⭐
- Как реализуются контейнеры наподобие `std::vector`, работающие с неинициализированной памятью? ⭐⭐⭐
- Зачем стандартная библиотека предоставляет алгоритмы `std::uninitialized_copy`, `std::uninitialized_move`,
- `std::destroy` и другие подобные функции? Приведите примеры их использования. ⭐⭐⭐
- В чём заключается разница между функцией `operator new` и оператором `new`? ⭐⭐⭐
- Чем отличаются средства управления памятью языка C (`malloc/free`) от механизмов C++ (`new/delete`)? ⭐⭐
- Почему смешивание пар `new/free` или `malloc/delete` приводит к неопределённому поведению? ⭐⭐
- Для каких типов объектов использование `memcpy` безопасно, а для каких может привести к ошибкам? ⭐⭐⭐
- Чем отличается `memmove` от `memcpy`? ⭐⭐
- В каких случаях применение `memset` к объектам C++ может быть опасным? ⭐⭐⭐
- Почему побайтовое копирование объектов не всегда эквивалентно их копированию через конструктор копирования? ⭐⭐⭐

## Ошибки при работе с памятью

- Что такое висячий указатель (dangling pointer)? Как он возникает? ⭐⭐
- Почему обращение через висячий указатель приводит к неопределённому поведению? ⭐⭐
- Что такое утечка памяти? Какие основные причины её возникновения? ⭐⭐
- Как потеря адреса динамически выделенного объекта приводит к утечке памяти? ⭐⭐
- Почему двойное удаление объекта считается серьёзной ошибкой? ⭐⭐
- Какие ошибки возникают при использовании неправильной пары операций выделения и освобождения памяти? ⭐⭐
- Какие инструменты и методы помогают обнаруживать утечки памяти и ошибки работы с памятью? ⭐⭐⭐
- Почему ручное управление памятью считается одной из самых сложных и опасных задач в программировании на C++? ⭐⭐⭐

## Современные практики управления памятью

- Почему в современном C++ рекомендуется минимизировать использование «сырых» указателей для владения объектами? ⭐⭐
- Какие преимущества дают контейнеры стандартной библиотеки по сравнению с динамическими массивами, созданными вручную? ⭐⭐
- Какие задачи решают умные указатели (`std::unique_ptr`, `std::shared_ptr`, `std::weak_ptr`)? ⭐⭐
- Как концепция RAII помогает избежать утечек памяти и других ресурсов? ⭐⭐⭐
- Почему управление памятью — это лишь частный случай более общей задачи управления ресурсами? ⭐⭐⭐
- Какие преимущества и недостатки имеют системы со сборкой мусора по сравнению с подходом C++? ⭐⭐⭐
- Почему наличие сборщика мусора не гарантирует отсутствие утечек памяти? ⭐⭐⭐
- Какие ресурсы, кроме памяти, требуют контроля времени жизни объектов? ⭐⭐
- В каких ситуациях ручное управление памятью всё ещё оправдано в современном C++? ⭐⭐⭐
- Какие принципы следует соблюдать, чтобы «не прострелить себе ногу» при работе с указателями и динамической памятью? ⭐⭐⭐

# Стандартная библиотека языка C++

## std::string и std::string_view

- Что представляет собой класс std::string в C++?
- Какие типы данных могут использоваться для хранения символов в std::string?
- Перечислите основные преимущества использования std::string по сравнению с C-строками.
- Опишите внутреннее устройство std::string в типичных реализациях?
- Какова разница между `std::string` и `std::wstring`?
- Какие операции можно выполнять над std::string?
- Чем отличается размер от вместимости?
- Что такое small string optimization?
- Как изменяются значения size и capacity при использовании метода erase?
- Какова сложность операций вставки и замены символов строки?
- Класс `std::string_view`, чем отличается от `std::string`?
- В каких случаях использовать `std::string_view`, а в каких `std::string`?
- Чем отличается substr в `std::string` и `std::string_view`?
- Как найти все вхождения некоторой подстроки в строке?
- Какой метод позволяет объединить две строки в C++?
- Как **быстро** найти все вхождения некоторой подстроки в строке?
- Какие операторы сравнения доступны для объектов `std::string`?
- Как преобразовать `std::string` в числовой тип данных и обратно?
- Как можно ускорить работу со строками в C++?
- Какой результат будет у `string_view` после разрушения оригинальной строки?

## std::vector и std::array

- Что такое `std::array` и как он отличается от C-массива?
- Можно ли передавать std::array по значению в функцию?
- Опишите трехмерный массив при помощи `std::array`.
- Что такое `std::vector`? Чем отличается от простого массива и `std::array`?
- Описать внутреннее устройство `std::vector`. Желательно показать на схеме.
- Что произойдёт с полностью заполненным `vector`, если выполнить операцию `push_back`?
- Типичные операции над `std::vector`. Какая сложность этих операций?
- Приёмы оптимизации производительности при работе с `std::vector`.
- Метод `at` и операция `[]`. В каких случаях какую из этих операций следует использовать?
- Как обойти все элементы `std::vector` в прямом и обратном направлении?
- Как `std::vector` хранит элементы в памяти: последовательно или с промежутками?
- Чем размер вектора отличается от его вместимости?
- Что такое инвалидация итераторов? Какие операции над `std::vector` приводят к инвалидации его итераторов?
- Что происходит с вектором при вызове `push_back`, если текущий размер равен ёмкости?
- Чем отличаются `emplace`-методы от `push`/`insert`?
- Идиома `erase`/`remove`.
- Как развернуть содержимое `std::vector` за O(1) памяти и O(N) времени?
- Как получить  указатель на первый элемент вектора?
- Какова амортизированная сложность операции `push_back`? Что такое амортизированная сложность?
- Как вставить в вектор элементы из другого контейнера, например из `deque` (в конец, в начало, в середину).
    Какова сложность этих операций?
- Для чего служит метод `shrink_to_fit()` и когда его стоит применять?
- Приведите пример использования обратных итераторов у вектора.
- Что происходит с данными в векторе при вызове `clear()`?
- Что происходит с итератором `end()`, если размер вектора изменяется?
- Может ли `std::vector` содержать объекты, для которых не определен оператор присваивания?
- Может ли `std::vector` содержать объекты, у которых отсутствует конструктор по умолчанию?
- Для чего нужен метод `reserve`?
- В каких случаях предпочтительнее использовать `std::vector`, а в каких — `std::list`?
- Какие ограничения у `std::vector` по сравнению с `std::deque`?
- Какой контейнер обеспечивает более быстрое вставку/удаление в середине: `std::vector` или `std::list`?

## std::deque

- Контейнер `std::deque`. Чем отличается от std::vector, в каких случаях применять vector, а в каких deque?
- Какой заголовочный файл необходимо подключить для работы с `std::deque`?
- Как устроен контейнер std::deque внутри? Нарисуйте схему и объясните её работу.
   Какие структуры данных используют типичные реализации этого контейнера?
- Почему размер блоков и количество блоков в `std::deque` обычно являются степенями двойки?  
- Какие операции над `std::deque` приводят, а какие не приводят к инвалидации итераторов? Объясните, с чем это связано?
- Для чего типичные реализации deque используют "закольцованность" блоков? 
- Основные операции над `std::deque` и их сложность.
- Чем отличаются emplace-методы от `push`/`insert`
- Отличия `std::deque` от `std::list`. Когда лучше применять `deque`, а когда `list`?
- Какие операции над std::deque приводят к инвалидации ссылок?
- Как развернуть std::deque?
- За счёт чего в `std::deque` обеспечивается доступ к элементам по индексу за O(1)?
- Как вставить в deque элементы из другого контейнера (в начало, в конец, в середину)?
- Какой метод `std::deque` позволяет безопасно получить элемент по индексу с проверкой границ?
- Почему в `std::deque` нет метода `data`, в отличие от `std::vector`?
- Какой получить ссылку на последний элемент `std::deque`?
- Какой из контейнеров более компактно хранит элементы в памяти: `std::deque` или `std::list`? Почему?
- Последовательная итерация по элементам какого из перечисленных контейнеров будет более быстрой:
    `deque`, `vector`, `list`?
- Как `std::deque` обрабатывает ситуацию, когда блоки заполнены?
- Сравните скорость вставки элементов в начало и в конец `std::deque` и `std::vector`.
- Сравните время доступа к элементу по его индексу в случае с `std::deque` и `std::vector`.
- В каких случаях `std::deque` предпочтительнее `std::vector`?
- Какие ограничения у `std::deque` по сравнению с `std::list`? 
- Какой контейнер обеспечивает более быстрое вставку/удаление в середине: `std::deque` или `std::list`?

## std::list и std::forward_list

- Контейнер `std::list` и `std::forward_list`, чем отличаются? Когда использовать `list`, а когда `forward_list`?
- Какие операции поддерживает `std::list`, а какие `std::forward_list`?
- Какие операции над `list` и `forward_list` приводят к инвалидации ссылок и итераторов, а какие нет? Почему?
- Как развернуть `std::forward_list`? Какова сложность этой операции по памяти и по времени?
- Как отсортировать `std::list`? Какова сложность этой операции?
- Какова временная сложность вставки элемента в произвольную позицию `std::list`?
- Как перенести элементы из одного списка в другой? Какова сложность этих операций?
- Как удалить элементы std::list, удовлетворяющие заданному критерию?
- Как удалить из std::list подряд идущие дубликаты? `[1, 3, 2, 2, -8, 5, 1, 1, 1, 2]` -> `[1, 3, 2, -8, 5, 1, 2]`
- Как перенести элементы из одного `forward_list` в другой. Какова сложность этих операций?
- Как отсортировать всё содержимое `forward_list`?
- Как отсортировать диапазон элементов `std::list` и `std::forward_list`?
- Как вставить в `std::list` диапазон элементов из другого контейнера?
- Какие операции можно выполнять над итераторами двусвязного списка?
- В каких случаях предпочтительнее использовать `std::list` вместо `std::vector`?
- Какой контейнер обеспечивает более быстрый доступ к элементу по индексу: `std::list` или `std::vector`?
- Каким образом можно вставить элементы двусвязного списка в другой контейнер в обратном порядке?
- Почему `std::forward_list` не имеет метода `push_back`?
- Как удалить из `std::forward_list` элемент, следующий за некоторым элементом?
- Как узнать количество элементов в `std::forward_list`?
- В каких случаях `std::forward_list` предпочтительнее `std::list`?

## Контейнеры map и multimap

- Контейнер std::map. Для чего используется?
- Операции над `map`. Какова сложность этих операций?
- В каком порядке находятся элементы `std::map`? Можно ли изменить порядок элементов? Если да, то как?
- Могут ли в качестве ключей `map` использоваться пользовательские типы данных? Что для этого нужно сделать?
- Каким требованиям должен удовлетворять тип данных, использующийся в качестве значений?
- Чем отличается вставка элементов c помощью `map[key] = value` от вставки `map.emplace(key, value)`?
- Чем отличается вставка элементов c помощью `map[key] = value` от вставки `map.at(key) = value`?
- Напишите функцию `optional<string> TryGetValue(const map<int, string>& m, int key)`
- Как можно ускорить вставку в `map`?
- Что происходит при использовании оператора `[]` для элемента, который ещё не существует в `std::map`?
- Как быстро перенести элемент из одного контейнера `map` в другой `map` того же типа?
- Как получить все значения `multimap` с заданным ключом?
- Что возвращает метод `insert` для `std::map` и как он отличается от `std::multimap`?
- В чём разница между `lower_bound` и `upper_bound`?
- Как реализовать пользовательскую функцию сравнения для `std::map`? 
- Какова временная сложность вставки, поиска и удаления в `std::map`?
- В каких случаях использовать `map`, а в каких `unordered_map`?
- Что происходит с итераторами при вставке нового элемента?
- Когда стоит использовать `std::multimap` вместо `std::map`?
- В чем различие между доступом к элементам с помощью метода `at` и операцией `[]`?
- Чем отличается `emplace` от `try_emplace`?
- Что происходит, если использовать `std::map` с ключами, не поддерживающими оператор `<`?
- В каком порядке возможен обход элементов контейнера map?
- Как удалить из контейнера map все элементы, удовлетворяющие заданному критерию?
- Как удалить из контейнера map не более N элементов, удовлетворяющих заданному критерию?

## Контейнеры unordered_map и unordered_multimap

- Контейнер `unordered_map`. Для чего используется?
- Операции над `unordered_map`. Их сложность.
- Что означает "амортизированная константа" применительно к сложности операций?
- В каком порядке хранятся элементы внутри `unordered_map`? Можно ли изменить порядок этих элементов?
- Как можно ускорить вставку в `unordered_map`?
- Чем отличается вставка элементов c помощью `umap[key] = value` от вставки `umap.emplace(key, value)`?
- Чем отличается вставка элементов c помощью `umap[key] = value` от вставки `umap.at(key) = value`?
- Напишите функцию `optional<string> TryGetValue(const unordered_map<int, string>& m, int key)`.
- Какие требования предъявляются к ключам для `std::unordered_map`?
- Что нужно сделать, чтобы в качестве ключей `unordered_map` использовать пользовательские типы данных?
- Что возвращает метод `insert` для `std::unordered_map` и как он отличается от `std::unordered_multimap`?
- В чём преимущество `std::unordered_map` перед `std::map`?
- Что такое хеш-функция? Как она используется в `unordered_map`?
- Как быстро перенести элемент из одного `unordered_map` в другой того же типа
- Что такое bucket-ы в `unordered_map`? Как они влияют на производительность?
- Что такое rehashing? Когда он возникает? Как можно им управлять?
- Приведите пример хешера для структуры с несколькими полями.
- Контейнер `unordered_multimap`. Чем отличается от `unordered_map`?
- Как реализовать пользовательский компаратор для `std::unordered_map`?
- Как получить все элементы `unordered_map` с одним и тем же ключом?
- Что произойдёт, если пользовательский хешер не обеспечивает равномерное распределение?
- В каком порядке возможен обход элементов `unordered_map`?
- Когда использовать map, а когда `unordered_map`?
- Как удалить из `unordered_map` не более N элементов, удовлетворяющих заданному критерию?

## Контейнеры set и multiset

- Контейнер std::set. Для чего используется?
- Операции над set. Какова сложность этих операций?
- В каком порядке находятся элементы std::set? Можно ли изменить порядок элементов? Если да, то как?
- Могут ли в качестве элементов set использоваться пользовательские типы данных? Если да, то что для этого нужно сделать?
- Каким требованиям должен удовлетворять тип данных, использующийся в качестве элементов set?
- Чем отличается вставке через insert от вставки через emplace? Когда какой метод использовать?
- Как быстро перенести один элемент из одного контейнера set в другой того же типа?
- Когда использовать set, а когда unordered_set?
- Как можно ускорить вставку в set?
- Как скопировать элементы из vector в set? Как скопировать элементы из set в vector?
- Контейнер multiset. Как получить все значения контейнера multiset с заданным ключом?
- Чем отличается insert от emplace?
- Как удалить из set все элементы, удовлетворяющие заданному критерию?
    Как удалить из set не более N элементов, удовлетворяющих заданному критерию?

## Контейнеры unordered_set и unordered_multiset

- Контейнер std::unordered_set. Для чего используется?
- Операции над unordered_set. Какова сложность этих операций?
- В каком порядке находятся элементы std::unordered_set? Можно ли изменить порядок элементов? Если да, то как?
- Могут ли в качестве элементов unordered_set использоваться пользовательские типы данных? Если да, то что для этого нужно сделать?
- Каким требованиям должен удовлетворять тип данных, использующийся в качестве элементов unordered_set?
- Чем отличается вставке через insert от вставки через emplace? Когда какой метод использовать?
- Как быстро перенести один элемент из одного контейнера unordered_set в другой того же типа?
- Когда использовать set, а когда unordered_set?
- Как скопировать элементы из vector в unordered_set? Как скопировать элементы из unordered_set в vector?
- Контейнер unordered_multiset. Как получить все значения контейнера unordered_multiset с заданным ключом?
- Как удалить из unordered_set все элементы, удовлетворяющие заданному критерию?
    Как удалить из unordered_set не более N элементов, удовлетворяющих заданному критерию?
- Что такое хеш-функция? Как она используется в unordered_set?
- Что такое bucket-ы в unordered_set? Как они влияют на производительность?
- Что такое rehashing? Когда он возникает? Как можно им управлять?

## std::function и лямбда-функции

- Что такое `std::function` и каковы его основные цели?
- Какие типы объектов может оборачивать `std::function`?
- В чём разница между `std::function` и указателем на функцию?
- Объявите указатель на функцию, которая принимает аргументы типа float и double и возвращает целое число.
- Что такое лямбда-функция и каков её синтаксис?
- Как захватить переменные из окружающего контекста в лямбда-функции?
- В чём разница между захватом по значению и по ссылке? Приведите примеры.  
- Можно ли изменить захваченные по значению переменные внутри лямбды?
- Что произойдёт, если захватить переменную по ссылке, которая вышла из области видимости? Приведите пример.
- Как захватить все переменные по значению, кроме одной по ссылке?
- Можно ли использовать `this` в лямбда-функции внутри класса?
- Приведите пример использования `std::function` для передачи функции-обработчика в другую функцию.
- Как использовать `std::bind` для создания функционального объекта с параметрами?
- Как избежать утечки памяти при использовании `std::function` с динамически выделенными объектами?
- Можно ли использовать `std::function` для привязки к методам класса?

## Ввод-вывод

- Как перенаправить вывод `std::cout` в файл при запуске программы?  
- Назовите три основных стандартных потока ввода-вывода в C++.
- Как проверить, успешно ли завершилась операция ввода?
- Какие флаги существуют у потоков ввода-вывода?
- Как вернуть поток в нормальное состояние после обнаружения ошибки?
- Как настроить потоки ввода-вывода, чтобы в случае ошибки выбрасывалось исключение?
- Как можно настроить поток ввода-вывода для ввода-вывода значений в другой системе счисления?
- Что происходит с `std::cin`, если ввести строку вместо числа?
- Как выровнять текст по правому краю с помощью манипуляторов?
- Как установить точность вывода чисел с плавающей точкой?
- В чём разница между режимами `std::ios_base::out` и `std::ios_base::app`?
- Как проверить, успешно ли открыт файл?
- Что происходит с содержимым файла при открытии в режиме `std::ios_base::trunc`?
- В чём разница между текстовым и двоичным режимами работы с файлами?
- Для чего используется `std::istringstream`?
- Как извлечь строку из `std::istringstream`?
- Как получить содержимое `std::ostringstream` в виде строки?
- Как убедиться, что данные, выведенные в поток были успешно сброшены на устройство?

## Итераторы

- Что такое итератор и для чего он используется?
- Какие основные категории итераторов вы знаете? Охарактеризуйте каждую.
- В чём разница между `LegacyInputIterator` и `LegacyOutputIterator`? 
- Что означает категория `LegacyContiguousIterator`? Чем отличается от `LegacyRandomAccessIterator`?
- Какой итератор позволяет многократный проход по контейнеру?
- Как получить итераторы для обратного прохода по C-массиву?
- Как получить итератор на начало и конец контейнера?  
- В чём преимущество `LegacyForwardIterator` перед `LegacyInputIterator`?
- Как компилятор разворачивает синтаксис `for (auto x : container)`?
- Как использовать `init-statement` в `range-based for` (начиная с C++20)?
- Чем отличаются категории итераторов, используемых в
    `std::map`, `std::unordered_map`, `std::forward_list`, `std::deque`, `std::vector`?
- Что такое инвалидация итераторов и в каких случаях она происходит?
- Как реализовать пользовательский итератор для собственного контейнера?
- В чём разница между итератором и указателем?

## Стандартные алгоритмы

- Как алгоритмы стандартной библиотеки взаимодействуют с контейнерами? 
- Сортировка элементов. Простая и стабильная сортировка. Алгоритмы частичной сортировки.
- Подсчёт элементов, удовлетворяющих заданному критерию.
- Поиск минимума и максимума.
- Нахождение медианы и процентилей.
- Поиск элементов. Последовательный. Двоичный поиск. Алгоритмы быстрого поиска всех вхождений подстроки в строке.
- Копирование и преобразование элементов.
- Алгоритмы над отсортированными множествами элементов: пересечение, объединение, разность множеств.
- Какой тип итераторов требуется для алгоритма `std::copy`?
- Для чего применяются проекции в `std::ranges::sort`?
- В чём разница между `std::any_of` и `std::all_of`?
- В чём заключается идиома erase-remove?
- Что происходит с элементами, которые "удаляются" при использовании `std::remove`?  
- Как при помощи алгоритма `std::copy` вывести элементы контейнера в обратном порядке?
- Как использовать `std::transform` для преобразования списка id пользователей в список пользователей?
- Что означают возвращаемые значения `std::lower_bound` и `std::upper_bound`?
- Как определить, сколько раз встречается элемент в отсортированном контейнере?
- Как реализовать копирование только нечётных чисел в новый контейнер?
- Как проверить, является ли массив отсортированным?
- Как найти медианное значение в контейнере?

## std::optional

- Для чего нужен класс `std::optional`?
- Как узнать, содержит ли объект `std::optional` значение?
- Как получить доступ к значению?
- Каковы основные преимущества `std::optional` по сравнению с указателями или возвратом кодов ошибок?
- Какие ошибки могут возникнуть при работе с классом `std::optional`?
- Напишите функцию, которая складывает два значения типа `optional<int>` и возвращает пустое значение,
   если хотя бы одно из слагаемых пустое, либо сумму чисел, если оба аргумента непустые.
- Как проверить, содержит ли `std::optional` значение?
- Что происходит при вызове `value()` для пустого `std::optional`?
- Что делает метод `emplace`? Приведите пример его использования.
- Что делает метод `value_or()`?
- Как использовать `std::optional` для возврата результата поиска в контейнере?
- Как реализовать отложенную инициализацию поля класса с помощью `std::optional`?
- Что происходит с `std::optional` при его присваивании другому значению?

## Стандартные умные указатели

- Что такое "use-after-free" и как умные указатели помогают избежать этой проблемы?
- Что такое `std::unique_ptr` и когда его следует использовать?  
- В чём разница между `std::unique_ptr` и `std::shared_ptr`? 
- Что такое RAII и как он связан с умными указателями?  
- Какой заголовочный файл содержит объявления `std::unique_ptr`, `std::shared_ptr` и `std::weak_ptr`? 
- В каких случаях предпочтительнее использовать `std::make_unique` и `std::make_shared`
   по сравнению с вызовом `std::shared_ptr<Type>(new Type())`?
- Как передать владение ресурсом из одного `std::unique_ptr` в другой?
- Как использовать `std::unique_ptr` для управления массивом объектов?
- Как создать в куче неинициализированный массив объектов и управлять им при помощи `std::unique_ptr`?
- По какому принципу `std::shared_ptr` управляет временем жизни объекта?
- Что такое "сильная" и "слабая" ссылка в контексте `std::shared_ptr`?
- Как создать `std::shared_ptr` с пользовательским deleter-ом?
- Какой узнать, содержит ли `std::shared_ptr` значение?
- В чём назначение `std::weak_ptr` и какую проблему он решает?
- Как получить `std::shared_ptr` из `std::weak_ptr`?
- Что происходит с `std::weak_ptr`, если объект, на который он ссылается, был удалён?
- Как проверить, существует ли объект, на который указывает `std::weak_ptr`? 
- Приведите пример использования `std::weak_ptr`.
- Зачем нужен класс `std::enable_shared_from_this`? Приведите пример его использования.
- Что произойдёт, если вызвать `shared_from_this()` в конструкторе объекта?
- В чём разница между `shared_from_this()` и `weak_from_this()`?
- Как реализовать идиому Pimpl с использованием `std::unique_ptr`?
- Приведите пример использования `std::unique_ptr` с пользовательским deleter-ом.
- Какой оверхед несёт `std::shared_ptr` по сравнению с `std::unique_ptr`?
- Какой подход выбрать для передачи умного указателя в функцию: по значению или по ссылке?
- Как преобразовать `std::shared_ptr<Derived>` к `std::shared_ptr<Base>` и обратно?
- Как внутри метода объекта, которым владеет `shared_ptr`, получить `shared_ptr`, ссылающийся на этот же самый объект?
    Для чего это может понадобиться?
- Как на основе `shared_ptr`, который владеет на объектом-агрегатом (например, структурой),
    получить `shared_ptr` на составную часть агрегата (например, поле структуры)?
    При этом объект-агрегат должен жить, пока жив либо указатель на часть, либо указатель на целое.

## std::filesystem

- Что такое `std::filesystem` и в каком заголовочном файле она определена?
- В чём разница между `std::filesystem::path` и обычной строкой для работы с путями?
- Как получить текущий рабочий каталог с помощью `std::filesystem`?
- Как объединить два пути в одном объекте `std::filesystem::path`?
- Как получить родительский каталог пути?  
- Как извлечь имя файла из полного пути?  
- Как получить расширение файла из пути?
- В чём разница между методами `string()` и `generic_string()`?
- Как проверить, существует ли файл или каталог?
- Как получить размер файла с помощью `std::filesystem`?
- Как создать новую директорию с помощью `std::filesystem`?
- Как скопировать файл из одного пути в другой?
- Как удалить файл или пустую директорию? 
- Как удалить директорию с содержимым? 
- Как обойти все файлы в директории?
- Как обойти рекурсивно все файлы в директории,
- Как нормализовать путь для разных ОС?
- Как проверить, является ли путь корневым?
- Как получить из абсолютного пути получить путь относительно некоторой директории?
- Как узнать, пусть указывает на файл или на директорию?

## std::variant

- Для чего нужен класс `std::variant`?
- Как проверить, содержит ли std::variant значение нужного типа или нет?
- Может ли std::variant не содержать значение?
- Как с помощью `std::visit` обработать все допустимые значения внутри `std::variant`?
- Для чего нужен `std::monotype`? Приведите пример.
- Реализуйте операцию `StringOrInt operator+(const StringOrInt& a, const StringOrInt& b)`,
   где тип `StringOrInt` — это `std::variant<std::string, int>`.
   Сложение должно происходить по правилам языка JavaScript:
  - 10 + 20 => 30
  - 10 + "20" => "1020"
  - "10" + 20 = "1020"
  - "10" + "20" = "1020"

# Объектно-ориентированное программирование

## Объектно-ориентированная парадигма программирования

- Что такое парадигма программирования? Чем она отличается от языка программирования? ⭐
- Какие преимущества даёт поддержка нескольких парадигм одним языком программирования? ⭐⭐
- В чём основные отличия процедурного и объектно-ориентированного подходов к разработке программ? ⭐⭐
- Какие проблемы больших программ пыталось решить процедурное программирование? ⭐⭐
- Почему структурное программирование стало важным этапом развития программирования? Какие проблемы решал отказ от goto? ⭐⭐
- В каких ситуациях объектно-ориентированный подход может быть предпочтительнее процедурного, а в каких — наоборот? ⭐⭐⭐
- Что такое объектно-ориентированное программирование? ⭐
- Что такое объект в ООП? Чем объект отличается от класса? ⭐
- Что означает утверждение, что программа моделирует предметную область через объекты? ⭐⭐
- Почему в ООП данные и операции над ними объединяются в одном типе? ⭐⭐
- Какие преимущества ООП проявляются при разработке крупных программных систем? ⭐⭐
- Какие недостатки или ограничения имеет объектно-ориентированный подход? ⭐⭐⭐

## Абстракция, инкапсуляция, инварианты

- Что такое абстракция данных? ⭐
- Чем абстракция отличается от инкапсуляции? ⭐⭐
- Почему абстракция считается одним из фундаментальных принципов ООП? ⭐⭐
- Что означает фраза «важно знать что делает объект, а не как он реализован»? ⭐⭐
- Какие преимущества даёт возможность изменять реализацию класса без изменения его интерфейса? ⭐⭐
- Приведите примеры хороших абстракций из стандартной библиотеки C++ или других языков программирования. ⭐⭐
- Что такое инкапсуляция? Почему согласно принципу инкапсуляции часто сравнивают с «чёрным ящиком»? ⭐
- Что такое интерфейс класса и чем он отличается от реализации? ⭐⭐
- Что такое инвариант объекта? Приведите примеры инвариантов. ⭐⭐
- Каким образом инкапсуляция помогает поддерживать инварианты класса? ⭐⭐
- Почему прямой доступ к данным объекта часто считается плохой практикой? ⭐⭐
- Какие преимущества даёт сокрытие реализации для сопровождения и развития программного обеспечения? ⭐⭐
- Может ли чрезмерная инкапсуляция ухудшать удобство использования класса? Приведите примеры. ⭐⭐⭐

## Наследование и полиморфизм

- Что такое наследование и какую задачу оно решает? ⭐
- Чем базовый класс отличается от производного? ⭐
- Какие преимущества даёт повторное использование кода через наследование? ⭐⭐
- В каких случаях наследование помогает лучше моделировать предметную область? ⭐⭐
- Какие недостатки и риски связаны с использованием наследования? ⭐⭐⭐
- Чем отношение «является» (is-a) отличается от отношения «содержит» (has-a)? ⭐⭐⭐
- Почему наследование иногда называют самым сильным видом связи между классами? ⭐⭐⭐
- Что такое полиморфизм? ⭐
- Какую проблему решает полиморфизм? ⭐⭐
- Почему полиморфизм часто используется совместно с наследованием? ⭐⭐
- Что такое общий интерфейс и какую роль он играет в полиморфизме? ⭐⭐
- Что такое виртуальная функция и зачем она нужна? ⭐⭐
- Чем статический полиморфизм отличается от динамического? ⭐⭐⭐
- Какие преимущества и недостатки имеет использование полиморфизма в крупных системах? ⭐⭐⭐
- Приведите примеры полиморфизма из стандартной библиотеки C++. ⭐⭐

## Классы, объекты, поля и методы

- Что такое класс? ⭐
- Что такое экземпляр класса? ⭐
- Почему класс называют пользовательским типом данных? ⭐
- Какие элементы обычно входят в состав класса? ⭐
- Чем класс отличается от структуры в C++? ⭐⭐
- В каких случаях использование структуры может быть предпочтительнее использования класса? ⭐⭐
- Что такое состояние объекта? ⭐
- Что такое члены-данные класса и какую роль они выполняют? ⭐
- Что такое метод класса? ⭐
- Чем метод отличается от обычной функции? ⭐⭐
- Почему методы имеют доступ к приватным данным объекта? ⭐⭐
- Какие операции обычно следует делать публичными, а какие — скрывать внутри класса? ⭐⭐
- Что такое высокоуровневый интерфейс класса? ⭐⭐⭐
- Какие уровни доступа существуют в C++ и для чего они используются? ⭐
- Чем отличаются public, private и protected? ⭐⭐
- Почему данные класса обычно делают private? ⭐⭐
- В каких случаях использование protected может быть оправдано? ⭐⭐⭐
- Какие проблемы могут возникать при чрезмерном использовании protected? ⭐⭐⭐

## Константность, свойства объектов, указатель this

- Что означает const для объекта? ⭐
- Что такое константный метод класса? ⭐
- Почему у константного объекта нельзя вызывать неконстантные методы? ⭐⭐
- Что такое физическая константность? ⭐⭐
- Что такое логическая константность? ⭐⭐
- Приведите примеры методов, которые логически не изменяют объект, но могут изменять его внутренние данные. ⭐⭐⭐
- Когда использование mutable является оправданным? ⭐⭐⭐
- Какие риски связаны с использованием mutable? ⭐⭐⭐
- Что такое свойство объекта? ⭐
- Чем свойство отличается от поля класса? ⭐⭐
- Почему getter не должен иметь заметных побочных эффектов? ⭐⭐
- Какие проблемы могут возникать, если чтение свойства изменяет состояние объекта? ⭐⭐
- Почему вычисляемые свойства часто предпочтительнее хранения избыточных данных? ⭐⭐⭐
- Что такое указатель `this`? ⭐
- Каким образом компилятор использует `this` при вызове методов? ⭐⭐
- В каких ситуациях необходимо явно использовать `this`? ⭐⭐
- Почему в большинстве методов `this` обычно не пишут явно? ⭐
- Что означает выражение `return *this` и где оно применяется? ⭐⭐

## Конструкторы и деструкторы

- Зачем нужны конструкторы? ⭐
- Почему объект должен находиться в корректном состоянии сразу после создания? ⭐⭐
- Что такое конструктор по умолчанию? ⭐
- В каких случаях компилятор автоматически генерирует конструктор по умолчанию? ⭐⭐
- Что такое список инициализации конструктора и зачем он нужен? ⭐⭐
- Для каких полей использование списка инициализации обязательно? ⭐⭐
- Почему инициализация через список инициализации может быть эффективнее присваивания в теле конструктора? ⭐⭐
- В каком порядке инициализируются поля класса? ⭐⭐⭐
- Что такое делегирующий конструктор и какие преимущества он даёт? ⭐⭐
- Что такое преобразующий конструктор? ⭐⭐
- Почему большинство конструкторов с одним параметром рекомендуется объявлять explicit? ⭐⭐
- Какие проблемы могут возникать из-за неявных преобразований типов? ⭐⭐⭐
- Что такое деструктор и какую задачу он решает? ⭐
- Когда вызывается деструктор объекта? ⭐
- Какие ресурсы обычно освобождаются в деструкторе? ⭐⭐
- В каком порядке разрушаются поля класса и базовые классы? ⭐⭐⭐
- В каких случаях автоматически сгенерированного деструктора достаточно? ⭐⭐
- В каких случаях необходимо реализовывать собственный деструктор? ⭐⭐
- Почему деструктор обычно не должен выбрасывать исключения? ⭐⭐⭐

## Копирование и присваивание объектов

- Что такое копирующий конструктор? ⭐
- В каких ситуациях вызывается копирующий конструктор? ⭐⭐
- Чем копирование отличается от присваивания? ⭐⭐
- Что такое поверхностное (shallow copy) и глубокое (deep copy) копирование? ⭐⭐⭐
- Почему автоматически сгенерированного копирующего конструктора иногда недостаточно? ⭐⭐⭐
- В каких случаях копирование объекта следует запретить? ⭐⭐⭐
- Что такое copy elision и почему эта оптимизация важна? ⭐⭐⭐
- Чем оператор присваивания отличается от копирующего конструктора? ⭐⭐
- Когда требуется реализовывать собственный оператор присваивания? ⭐⭐
- Почему оператор присваивания обычно возвращает ссылку на текущий объект? ⭐⭐
- Что такое самоприсваивание и почему его нужно учитывать? ⭐⭐⭐
- В чём заключается идея идиомы copy-and-swap? ⭐⭐⭐

## Семантика перемещения

- Какие проблемы копирования решает семантика перемещения? ⭐⭐
- Что такое lvalue и rvalue? ⭐⭐
- Чем отличаются ссылки T& и T&&? ⭐⭐
- Что делает std::move? ⭐⭐
- Почему название std::move может вводить в заблуждение? ⭐⭐⭐
- Что такое конструктор перемещения? ⭐⭐
- Что происходит с объектом после перемещения из него ресурсов? ⭐⭐⭐
- В каких случаях перемещение даёт существенный выигрыш по производительности? ⭐⭐
- Почему перемещение особенно полезно для классов, управляющих ресурсами? ⭐⭐⭐
- Когда реализация move-конструктора не имеет практического смысла? ⭐⭐⭐
- В чём заключается правило пяти? ⭐⭐
- В чём заключается правило нуля и почему оно считается предпочтительным в современном C++? ⭐⭐⭐

## Статические методы и данные

- Что такое статический метод класса? ⭐
- Чем статический метод отличается от обычного метода? ⭐⭐
- Почему статические методы не имеют указателя this? ⭐⭐
- В каких случаях метод следует сделать статическим? ⭐⭐
- Что такое статическое поле класса? ⭐⭐
- Чем статическое поле отличается от обычного поля объекта? ⭐⭐
- Какие преимущества дают статические поля для хранения общих данных класса? ⭐⭐
- Приведите примеры использования статических членов в стандартной библиотеке C++. ⭐⭐

## Проектирование классов

- Какие характеристики хорошего класса можно выделить? ⭐⭐⭐
- Почему не рекомендуется хранить данные, которые можно вычислить из других данных? ⭐⭐⭐
- Как связаны инкапсуляция и снижение связности между компонентами системы? ⭐⭐⭐
- Какие вопросы следует задать себе при проектировании нового класса? ⭐⭐⭐
- В каких случаях стоит предпочесть композицию наследованию? ⭐⭐⭐
- Как связаны между собой абстракция, инкапсуляция, наследование и полиморфизм? ⭐⭐⭐
- Почему говорят, что объект должен сам отвечать за корректность своего состояния? ⭐⭐⭐
- Каким образом конструкторы, деструкторы и инкапсуляция совместно обеспечивают надёжность класса? ⭐⭐⭐
- Какие принципы ООП наиболее важны для сопровождения больших программных систем и почему? ⭐⭐⭐
- Какие идеи объектно-ориентированного программирования остаются актуальными независимо от конкретного языка программирования? ⭐⭐⭐

## Отношения между классами

- Объясните разницу между **ассоциацией**, **агрегацией**, **композицией** и **зависимостью**;
   приведите по одному примеру каждого отношения.
- Как композиция, агрегация, зависимость и ассоциация обозначаются на диаграммах классов.
- В каких случаях ассоциация бывает двусторонней, а в каких — односторонней? Как это отражается в коде и на диаграмме?
- Сравните ответственность Целого в композиции и агрегации: что происходит с Частью при уничтожении Целого в каждом случае?
- Какие преимущества даёт предварительная визуализация архитектуры программы на диаграмме классов?
   Опишите минимум три сценария, когда это особенно полезно.
- Почему принцип «информация скрыта» особенно важен при композиции?
   Опишите потенциальные ошибки проектирования, если Целое отдаёт наружу ссылки на свои Части.
- Опишите процесс эволюции связей: как можно безопасно заменить агрегацию компози­цией,
   если Целое решило единолично владеть Частью? Какие изменения потребуются в коде?
- Почему композиция считается более "сильной" связью, чем агрегация?
- Предложите критерии, по которым вы определяете, какой вид отношения использовать при проектировании новой пары классов.
- Почему зависимость считается «самой слабой» связью? Как эта слабость отражается на гибкости и расширяемости системы?
- Что такое «делегирование» в контексте отношений между классами?
    Чем оно отличается от простого вызова метода ассоциированного объекта клиентом?
- Опишите потенциальные проблемы, когда одна из сторон ассоциации хранит
    «сырые» указатели на другую сторону вместо умных указателей.
- Приведите пример, когда агрегация логически превращается в зависимость во время рефакто­ринга.
    Какие изменения в коде это отражают?

## Наследование - 1

- Дайте определение отношения «является» («is-a») и приведите пример его реализации на C++.
- Объясните различие между отношениями «is-a» и «has-a». Приведите по одному примеру кода.
- Что такое класс-родитель, класс-потомок и базовый класс?
- В чём смысл понятия *иерархия наследования* и как она обычно изображается на UML-диаграммах?
- Перечислите преимущества кода, достигаемые с помощью наследования. Назовите минимум два недостатка.
- Что хранит таблица виртуальных функций (v-table) и как она используется при динамическом полиморфизме?
- Приведите пример, когда метод базового класса **не** должен быть виртуальным, и обоснуйте решение.
- Почему компилятор вызывает конструктор базового класса **перед** конструктором производного?
- Как вызвать параметризованный конструктор базового класса из конструктора наследника?
- Что нужно сделать в конструкторе класса-наследника, если в классе-родителе нет конструктора по умолчанию?
- Чем отличается *переопределение* (override) виртуального метода от *скрытия* (method hiding)?
- В каких случаях композиция предпочтительнее наследования?
- Как спецификатор `override` помогает выявить несоответствие сигнатур?
- Что произойдёт, если пометить метод как `virtual`, но забыть `override` в наследнике?
- Вызов конструкторов и деструкторов при наследовании.

## Наследование - 2

- Каковы особенности публичного, приватного и защищённого наследования? Когда их следует применять?
- К каким полям и методам класса может иметь доступ класс-наследник?
- В каком случае возможно приведение типа вверх по иерархии классов.
    Как влияет тип наследования на приведение типа по иерархии наследования?
- Что произойдёт, если в классе-наследнике будет объявлен метод с тем же именем, что в одном из его родителей?
- Как класс-наследник может вызвать одноименный метод своего родителя?
- Чем отличается  `class X final { /*...*/ };` от `class X { /*...*/ };`. Когда следует объявлять класс или структуру финальной?
- Как в функцию `void F(Base&)` передать ссылку на объект класса `class Derived : private Base {...};`?
- Что произойдёт, если класс-наследник попытается получить доступ к `protected`-члену через объект **другого** наследника?
- Может ли интерфейс (класс с чисто виртуальными методами) содержать `protected`-методы? Приведите аргументы «за» и «против».
- Когда оправдано использование `protected`-деструктора?
- Как сделать так, чтобы `protected`-члены базового класса в итоге стали **приватными** в производном?
- Приведите два преимущества и два недостатка размещения полей в `protected` вместо `private`.
- Как класс может запретить своим наследникам переопределя некоторые из его виртуальных методов?
- Изобразите на UML диаграмме класс с одним `public`, одним `protected` и одним `private` методом.
- Существует мнение, что «раннее закрытие» иерархии `final`-классами снижает тестируемость и расширяемость кода.
    Приведите аргументы «за» и «против» с точки зрения (а) модульного тестирования, (б) библиотечного API.

## Наследование и жизненный цикл объектов

- В каких случаях деструктор вызывается автоматически, а в каких – явно программистом?
- Объясните, почему деструктор базового класса, от которого наследуются полиморфные типы, чаще всего объявляется виртуальным.
- Что такое тривиальный деструктор? Перечислите условия, при которых компилятор сгенерирует именно такой деструктор.
- Напишите пример кода, в котором отсутствие виртуального деструктора в базовом классе приводит к проблемам.
- Приведите пример архитектуры, где **публичный невиртуальный** деструктор базового класса является корректным решением
   и не вызывает рисков.
- Расскажите, как использование `std::unique_ptr<Base>` влияет на требования к деструктору `Base`.
- Чем различаются `delete` и «уничтожение» объекта при выходе из области видимости?
   Прокомментируйте в контексте деструкторов и наследования.
- Опишите правила наследования виртуальности деструктора:
   как объявление виртуального деструктора в базе отражается на всех производных классах?
- Объясните назначение **защищённого невиртуального** деструктора.
    Как он препятствует неправильному использованию базового класса?
- В чём отличие вызова `delete this;` внутри метода класса с виртуальным деструктором и без него? Распишите возможные ошибки.
- Сформулируйте «правило пяти» и объясните,
    как виртуальные деструкторы влияют на необходимость явно объявлять другие специальные функции.
- Приведите пример класса, для которого **конструктор** сделан `protected`, а деструктор — `public virtual`.
    Объясните дизайн-решение.
- Какие проблемы могут возникнуть при наследовании от сторонней библиотеки,
    если базовый класс не имеет виртуального деструктора? Предложите стратегию обхода.
- Напишите юнит-тест (на любом тестовом фреймворке C++) для проверки корректного разрушения коллекции
    `std::vector<std::unique_ptr<Shape>>`, содержащей разные фигуры.
- Опишите влияние виртуального деструктора на размер объекта и layout памяти. Какие поля добавляются компилятором?
- Предложите способ запретить удаление объекта через указатель на базовый класс без отказа от полиморфного интерфейса.
- Каковы особенности работы виртуальных методов во время работы конструктора и деструктора?
- Предположим, что библиотека распространяет базовый класс `Widget`, методы которого не помечены `final`.
    Опишите процесс миграции на новую версию библиотеки, в которой те же методы объявлены `final`.
    Какие потенциальные проблемы ожидают разработчика пользовательского кода и как их минимизировать?
- Укажите четыре сценария, при которых необходимо явно определять деструктор производного класса даже тогда,
    когда базовый имеет виртуальный деструктор по умолчанию.

## Виды наследования

- Сформулируйте различия между публичным, защищённым и приватным наследованием
   с точки зрения доступа к членам базового класса.
- Объясните, почему публичное наследование называют «наследованием интерфейса»,
   и приведите пример корректного применения этого принципа.
- Назовите признаки, по которым можно определить, что выбранное публичное наследование применено некорректно.
- Охарактеризуйте основное семантическое отличие композиции от приватного наследования при повторном использовании кода.
- Почему класс-наследник, полученный через приватное наследование, не является подтипом своего базового класса?
- Почему почти все пользовательские классы-исключения наследуются от `std::exception` именно публично?
   Объясните, какие проблемы возникнут, если заменить это наследование на защищённое или приватное,
   и как это отразится на механизме перехвата исключений.
- Проанализируйте следующие фрагменты кода и объясните результат компиляции и/или выполнения:

    ```cpp
    class MyLibError : private std::exception {
    public:
        const char* what() const noexcept override { return "lib error"; }
    };

    void Foo() { throw MyLibError(); }

    int main() {
        try {
            Foo();
        } catch (const std::exception& e) {          // 1
            std::cout << e.what();
        } catch (const MyLibError& e) {              // 2
            std::cout << e.what();
        }
    }
    ```

- Объясните, как закрытое наследование влияет на дружественные функции и классы.
- Может ли класс, унаследованный защищённо, стать базовым публичным классом для своего наследника?
  Что это означает для внешнего кода?
- Как динамическое приведение типа (`dynamic_cast`) ведёт себя при публичном и при непубличном наследовании.
- Почему принято считать, что композиция часто предпочтительнее наследования?
- Объясните, как ключевое слово `using` помогает «поднимать» скрытые члены базового класса при приватном наследовании.
- Как protected-наследование влияет на возможность повторного использования защищённых методов базового класса
    в более глубоких иерархиях?

## Интерфейсы и абстрактные классы

- Что такое **абстрактный класс** в C++ и какие условия заставляют компилятор считать класс абстрактным?
- Объясните, почему экземпляры абстрактного класса нельзя создать напрямую, и опишите способы,
   которыми абстрактный класс всё-таки может участвовать в создании объектов.
- Что такое **чисто виртуальная функция**? Сравните её с обычной виртуальной функцией и приведите пример,
   демонстрирующий различия в использовании.
- Опишите назначение виртуального деструктора в абстрактном классе. Какие риски возникают, если деструктор объявлен невиртуальным?
- Когда абстрактному классу имеет смысл предоставлять реализованные (нечистые) методы?
   Укажите, какие преимущества и какие потенциальные проблемы это создаёт.
- В чём состоит принцип «программируй на уровне интерфейса, а не реализации»? Как абстрактные классы помогают его соблюдать?
- Сравните **абстрактный класс** и **интерфейс** как крайний случай абстрактного класса.
- Когда в C++ имеет смысл реализовывать интерфейс приватным или защищённым образом? Приведите примеры.
- Приведите пример, когда имеет смысл в одном классе реализовывать несколько интерфейсов?
- Опишите механизм, с помощью которого класс-реализация документирует своё соответствие интерфейсу.
    Какие инструменты языка служат для контроля этого соответствия.
- Объясните, как интерфейсы помогают снизить связанность модулей и упростить написание модульных тестов.
- Что произойдёт, если класс-наследник **не реализует** один из чисто виртуальных методов интерфейса?
- Можно ли в интерфейсе объявить статические методы?
    Приведите пример и обсудите, насколько это соответствует идее интерфейса.
- Поясните, как «интерфейс» в C++ отличается от такового в Java или C#.
    Какие возможности и ограничения добавляет отсутствие отдельного ключевого слова `interface`?

## Приведение типов по иерархии наследования

- Объясните понятия **up-casting** и **down-casting** в контексте иерархии классов.
   В чём заключается принципиальная разница в их безопасности?
- Почему приведение типа **вверх** по иерархии (от `Derived*` к `Base*`) компилятор выполняет неявно,
   тогда как приведение **вниз** требует явного указания?
- Что такое "Срезка" и при каких формах приведения типов она проявляется?
- В каких ситуациях использование `static_cast` для down-cast а безопасно, а в каких приводит к неопределённому поведению?
- Почему базовый класс должен иметь хотя бы один виртуальный метод для корректной работы `dynamic_cast`?
- Как ведёт себя `dynamic_cast` при приведении указателя?
- Чем отличается поведение `dynamic_cast` при приведении ссылки?
- Объясните, почему частое использование `dynamic_cast` может указывать на плохое проектирование системы классов.
- Как можно уменьшить потребность в down-cast ах, сохранив расширяемость и открытость кода?
- Что произойдёт, если выполнить `reinterpret_cast<Derived*>(&base)` вместо `dynamic_cast`? Разберите потенциальные риски.
- Как изменение доступа (`public`, `protected`, `private`) при наследовании влияет на возможность неявного up-cast а?
- Как выполнить безопасное приведение типа `std::shared_ptr<Base>` к `std::shared_ptr<Derived>`?
- Почему в production-коде часто предпочитают проверку `if (auto d = dynamic_cast<Derived*>(ptr))`
    вместо безусловного приведения ссылки?
- Вы разрабатываете плагин-систему, где функции принимают базовый интерфейс `IPlugin&`.
    Предложите архитектурное решение, позволяющее модулю определить, 
    поддерживает ли плагин дополнительный интерфейс `IRenderable`, минимизируя использование `dynamic_cast`.

## Множественное наследование

- Что такое множественное наследование и какие практические задачи оно решает в отличие от одиночного наследования?
- Чем различаются множественная реализация интерфейсов и множественное наследование реализаций? Приведите примеры.
- В каком порядке вызываются конструкторы и деструкторы при создании объекта,
   наследующегося от двух базовых классов без виртуального наследования?
- Почему при множественном наследовании часто рекомендуется явно переопределять методы,
   даже если классы-родители уже предоставляют собственные версии?
- Приведите пример использования множественного наследования для одновременной реализации нескольких интерфейсов.
- Как ключевое слово `override` помогает выявлять ошибки при множественном наследовании?
- Объясните, почему множественное наследование усиливает *контракт* между базовыми и производными классами
   и как это может усложнить поддержку кода.
- В чём разница между следующими объявлениями?

    ```cpp
    class D : public A, public B {};
    class D : public virtual A, public virtual B {};
    ```

- Почему при виртуальном наследовании требуется явный вызов конструктора виртуальной базы на самом нижнем уровне иерархии?
- Рассмотрите дочерний класс, который наследует `public A`, `private B`.
    Какие части интерфейса каждой базы становятся доступны извне?
- Какие проблемы возможны при множественном наследовании? Какие у них существуют способы решения?

## Runtime-полиморфизм с std-variant

- Что представляет собой шаблонный класс `std::variant`, и чем он принципиально отличается от традиционного C-style `union`?
- Какие требования предъявляет `std::variant` к наборам типов-альтернатив, и как компилятор гарантирует типобезопасность?
- Объясните, что происходит внутри объекта `std::variant`: как он хранит данные разных размеров и как определяет,
   какой тип активен в данный момент.
- Что делает структура‐маркер `std::monostate` и почему её часто располагают первой в списке типов?
- Какие плюсы и минусы даёт `std::variant` по сравнению с классической иерархией с виртуальными методами,
   когда речь идёт о производительности и размере объекта?
- Как работает механизм visitor в контексте `std::variant`? Охарактеризуйте роль функции `std::visit`.
- Сравните три способа доступа к данным внутри `variant`:
   `std::visit`, `std::holds_alternative` + `std::get`, `std::get_if`. Когда и какой метод предпочтителен?
- Почему функция `std::visit` требует, чтобы посетитель обрабатывал все возможные альтернативы?
   Что произойдёт при нарушении этого требования?
- Объясните, как *generic lambda* может заменить класс-посетитель. Приведите краткий пример.
- Что такое паттерн **`overloaded`** и как он упрощает создание посетителей с помощью композиции лямбда-функций?
- В каких случаях `std::variant` может уступать виртуальным методам по скорости? Опишите факторы, влияющие на это.
- Разберите пример: `using Token = std::variant<int, double, std::string>;`.
    Сколько байт займёт `Token` на 64-битной платформе и почему?
- Как добавить новую операцию над всеми вариантами без изменения существующего кода альтернатив?
- Как можно обработать два (или более) `std::variant` одновременно? Приведите пример.
- Представьте систему плагинов, где типы добавляются динамически.
    Оцените, насколько хорошо `std::variant` подходит для такой архитектуры.
- Какие меры нужно предпринять, чтобы уменьшить потенциальное «раздувание» размера `variant`,
    если один из типов альтернаты значительно крупнее других?

## Перегрузка операций - 1

- В каких случаях перегрузку бинарного оператора стоит объявлять внутри класса, а в каких — вне класса?
- Почему симметричные бинарные операции (`a + b`, `a == b`) часто реализуют как свободные функции?
- Опишите различия между объявлением бинарного оператора как дружественной функции и как обычной свободной функции.
   Когда дружба оправдана с точки зрения инкапсуляции?
- Какие ограничения накладывает стандарт C++ на перегрузку операторов?
- Чем перегрузка оператора `+=` внутри класса упрощает реализацию оператора `+`?
   Покажите паттерн «определи `+=`, а `+` реализуй через копию и `+=`.»
- Что такое неявно-генерируемый оператор присваивания,
   и в каких случаях вам потребуется ручная перегрузка `operator=` после добавления арифметических операторов?
- Объясните назначение и преимущества оператора `<=>` (spaceship) в C++20.
   Какие виды порядка существуют и когда каждый следует использовать?
- В чём разница между `std::strong_ordering` и `std::weak_ordering`?
   Приведите пример типа данных, для которого подходит только слабый порядок.
- Рассмотрите ситуацию с «поглощением» исключений внутри перегруженных операторов. Какие альтернативы вместо silent‐fail?
- Приведите пример перегрузки операций сложения и умножения для комплексных чисел.
- Как перегрузить `operator*` для класса `Complex`, чтобы поддержать и скалярное, и комплексное умножение?

## Перегрузка операций - 2

- В чём семантическое различие между операторами `*` и `->` у «умного» указателя? 
   Что должен возвращать каждый из них, чтобы объект вёл себя как обычный указатель?
- Почему для пользовательских типов перегрузку `operator<<` и `operator>>` всегда объявляют как свободные функции,
   а не методами класса потока?
- В каком порядке (левый → правый операнд) вызываются перегрузки `operator<<` при цепочке `std::cout << a << b;`?
   Как это влияет на сигнатуру оператора?
- Покажите сигнатуру перегруженного оператора вывода `<<` для класса `Rational`.
   Объясните, зачем он должен возвращать `std::ostream&`.
- Какие ограничения накладывает стандарт на возвращаемое значение `operator->()`?
   Что произойдёт, если вернуть объект по значению вместо указателя?
- Почему `operator<<` часто объявляется `friend`-функцией пользовательского класса?
   Какие преимущества и какие проблемы с инкапсуляцией это создаёт?
- Напишите минимальный код нестандартного «умного» указателя `UniquePtr<T>`,
   показывающий реализацию `operator*` и `operator->` с проверкой на `nullptr`.
- Что такое ADL (argument-dependent lookup) и как оно срабатывает при вызове `std::cout << myObj;`?

## Перегрузка операций - 3

- Что такое пользовательский литерал и какие задачи он решает?
- Каковы правила написания суффикса пользовательского литерала? Почему стандарт требует начинать его с символа подчёркивания?
- Почему перегрузка литерала для строк должна принимать два аргумента (`const char*`, `std::size_t`)
- Продемонстрируйте как с помощью пользовательских литералов и шаблонов можно отличить значения разных единиц измерения
   (метры, сантиметры) и тем самым предотвратить логические ошибки при арифметике.
- Какие ограничения накладывает стандарт C++ на место объявления оператора `[]`?
- Объясните различие между «читающей» и «записывающей» версиями `operator[]`.
- Начиная с C++23 `operator[]` может принимать несколько индексов. 
   Опишите синтаксис и потенциальные области применения этой возможности.
- До C++23 для имитации `matrix[i][j]` использовали «прокси-объект» (row proxy).
   Объясните, как он работает и какие недостатки устраняет новый стандарт.
- В чём семантическая разница между префиксным `++it` и постфиксным `it++` для итератора?
    Как это отражается в сигнатурах перегруженных операторов?
- Опишите, как можно реализовать «циклический» контейнер,
    в котором `operator[]` автоматически оборачивает индекс по модулю размера.
- Как должны вести себя перегруженные `--` у итератора двунаправленного списка,
    чтобы удовлетворять требованиям категории BidirectionalIterator?

## Перегрузка операций - 4

- Каков общий синтаксис объявления оператора неявного приведения типа в классе?
- Какие риски создаёт наличие неявного оператора приведения типа? Опишите типичный сценарий ошибки.
- Допустимо ли объявить оператор приведения с модификатором `explicit`? 
   Чем он отличается от неявного и в каких ситуациях это оправдано?
- Почему для оператора приведения типов не указывают возвращаемое значение в списке параметров функции?
- Объясните опасность перегрузки оператора приведения к ссылочному типу (например, `operator int&()`)?
- Чем отличается оператор приведения к пользовательскому типу от конвертирующего конструктора?
- В чём преимущества функционального объекта (перегрузка `operator()`) перед обычной свободной функцией?
- Объясните, почему «объект функции» традиционно быстрее указателя на функцию при использовании в алгоритмах STL.
- Какие ограничения накладываются на количество аргументов и спецификаторы доступа при перегрузке `operator()`?
- Можно ли перегрузить `operator()` в классе-шаблоне с разным набором параметров (overload set)?
- Опишите процесс передачи функции-объекта в `std::for_each`. Какие требования к типу (concepts) должен удовлетворять функтор?
- В чём различие между лямбда-выражением и объектом класса с перегруженным `operator()`?
    Приведите пример, когда объект класса предпочтительнее.
- Какие проблемы могут возникнуть, если функтор хранит внутренние указатели на внешние ресурсы? Как их избежать?
- Возможно ли создать константный и неконстантный вариант `operator()` в одном классе? Зачем это бывает нужно?

## Обработка исключений - 1

- Какие виды непредвиденных ситуаций могут возникать во время работы программы и почему важно обрабатывать каждую из них?
- Почему игнорирование ошибки (отсутствие обработки) считается самым плохим решением, и какие последствия это может вызвать?
- Почему глобальная переменная с кодом ошибки непригодна для многопоточных программ?
- Опишите механизм поиска подходящего обработчика (`catch`) после выполнения `throw`.
   Как влияет порядок блоков `catch` на поведение программы?
- Что такое «раскрутка стека» (stack unwinding) и какие действия выполняются во время неё?
- Чем отличается выражение `throw;` от `throw expr;`? В каких случаях применяется каждое из них?
- Как указать, что метод или функция не выбрасывают исключений?
- Опишите жизненный цикл объекта исключения: от момента создания до уничтожения.
   Какие конструкторы и деструкторы должны быть доступны?
- В каких случаях спецификатор `noexcept` улучшает производительность контейнеров STL?
- Почему рекомендуется перемещающий конструктор и оператор присваивания делать не выбрасывающими исключений?
- Как обработать ситуацию, когда внутри `noexcept`-функции возникает ошибка,
    которую необходимо «доставить» вызывающему коду?

## Обработка исключений - 2

- Что такое срезка? Как избежать срезки при обработке исключений?
- Когда стоит создавать собственный класс исключений, а когда — использовать существующие стандарта?
   Как правильно спроектировать иерархию своих исключений?
- Как с помощью `try/catch` обеспечить откат частично изменённого состояния при возникновении исключения?
- В чём различие между обработчиками `catch (const std::exception&)` и `catch (...)`?
   В каких случаях стоит добавлять «любой» обработчик?
- К каким последствиям приводит выброс исключения в конструкторе класса?
- Какие средства стандартной библиотеки позволяют узнать,
    что у класса или структуры конструктор копирования/перемещения не выбрасывает исключений.
- Для чего служит класс `std::nested_exception`? Приведите пример использования.
- Как перехватить все исключения стандартной библиотеки?
- Опишите подход, позволяющий объединить проверку ошибок возвращаемыми значениями и механизм исключений так,
    чтобы предоставить пользователю гибкий выбор стратегии.
- Расскажите, как осуществляется обработка исключений при конструировании объекта,
    если исключение выбрасывается в списке инициализации.

## Разработка кода, устойчивого к исключениям - 1

- Сформулируйте четыре уровня гарантии безопасности исключений и опишите,
   какие обязанности возлагает каждый уровень на обеспечивающую его функцию или метод.
- Почему «базовый уровень безопасности» считается минимально приемлемым для промышленного кода?
   Приведите пример, когда его достаточно, и пример, где нужен более высокий уровень.
- Что означает «commit-or-rollback»-семантика?
- Объясните, какие проблемы могут возникнуть при отсутствии безопасности исключений, даже если программа не падает немедленно.
- Как спецификатор `noexcept` влияет на выбор алгоритмов внутри контейнеров стандартной библиотеки.
- При каких условиях перемещающий конструктор стоит объявлять `noexcept`, а когда — `noexcept(false)`?
- Почему деструкторы по умолчанию объявлены `noexcept`?
   Что произойдет, если деструктор всё-таки выбросит исключение во время раскрутки стека?
- Приведите пример, показывающий, как нарушение инвариантов объекта после выброса исключения
   может привести к неопределённому поведению.
- Как `BOOST_SCOPE_EXIT` или `gsl::finally` помогают писать устойчивый к исключениям код?
- Сформулируйте основную идею идиомы RAII и объясните, каким образом она упрощает обеспечение
    хотя бы базового уровня безопасности исключений.
- Чем отличается объект-сторож (scope guard, вроде gsl::finally или BOOST_SCOPE_EXIT) от классического RAII-объекта?
    Приведите примеры ситуаций, где удобнее применить первый вариант.

## Разработка кода, устойчивого к исключениям - 2

- Как использование `std::make_shared` одновременно решает проблему утечки и
    повышает производительность? Назовите недостаток этого решения.
- Почему использование `std::unique_ptr` и `std::shared_ptr` считается «готовым» решением RAII?
    Назовите ограничения, при которых этих умных указателей недостаточно для строгой гарантии.
- Приведите пример функции, обеспечивающей «сильную» гарантию, и объясните, какие техники в ней используются.
- В чём различие между «исключение в списке инициализации» и «исключение в теле конструктора
    с точки зрения освобождения ресурсов полей класса?
- Опишите методику анализа функции на предмет безопасности исключений:
    как выявить потенциально опасные операции и как реструктурировать код.
- Как в модульных тестах можно смоделировать генерацию исключений для проверки exception-safety компонентов.
-  Опишите, как «инвариант класса» связан с понятием базовой  безопасности исключений.
    Приведите пример класса, в котором нарушение инварианта делает объект непригодным к использованию.
- Что такое «гарантия отсутствия побочных эффектов»
    и почему иногда её невозможно обеспечить без кардинальных изменений в архитектуре?
- Что означает выражение «код, вызываемый извне приложения, должен быть `noexcept`», и к каким категориям кода это относится?
- Приведите пример, когда сгенерированный компилятором оператор присваивания работает корректно в отсутствие исключений,
    но не обеспечивает даже безовый уровень безопасности при выбрасывании исключений.
- Почему операция присваивания в классе `std::vector` обеспечивает лишь базовый уровень безопасности, а не строгий?

## Обобщённое программирование - 1

- Как объявить шаблон функции?
- Опишите, как компилятор выбирает, когда инстанцировать шаблон функции. Что такое «точка инстанцирования»?
- Может ли шаблонная функция не иметь аргументов?
- В чём состоит требование «одно определение» (ODR) для шаблонов и почему код шаблонов обычно помещают в заголовки?
- Приведите пример, когда перегрузка функции предпочтительнее полной специализации шаблона.
- Что может использоваться в качестве параметров шаблона? Приведите пример.
- Объясните роль `constexpr if` в обобщённом программировании. Как он облегчает написание шаблонов?
- Для чего используется специализация шаблонов функций? Приведите пример специализации шаблонной функции.
- Объясните отличие между шаблонной функцией‐оператором (`operator<<`) и обычной функцией‐оператором для конкретного потока.
- Что означают термины «первичный (primary) шаблон» и «специализация»?
- Как объявить специализацию шаблонной функции?
- Может ли специализация шаблонной функции сама быть шаблоном? Если да, приведите пример.
- Какие риски несёт чрезмерное использование шаблонов?
- Как объявляются шаблонные операторы? Приведите пример.
- В каких случаях разумнее использовать перегрузку вместо специализации (или наоборот) при настройке поведения универсального алгоритма?

## Шаблоны классов

- Как объявить шаблон класса?
- Для каких целей используется частичная специализация шаблонов классов?
   Приведите пример.
- Шаблон `CSimpleArray<T, SIZE>` хранит размер в параметре шаблона.
   Какие плюсы и минусы такого решения по сравнению с хранением размера как поля?
- Как объявляется метод шаблонного класса вне класса?
- Как объявляется шаблонный метод класса вне класса?
- Опишите CRTP (Curiously Recurring Template Pattern) и приведите пример его применения.
- Как шаблоны позволяют реализовать статический полиморфизм? В чём отличие от виртуальных функций?
- Как объявляется шаблонный метод шаблонного класса вне класса?
- Чем отличается частичная и полная специализация шаблона класса. 
- Как объявить частичную и полную специализацию шаблона класса?
- Может ли виртуальный метод класса быть шаблонным?
- Применение шаблонов при построении иерархий классов.
- В чем отличие между `template`-параметрами-шаблонами (template template parameters) и обычными типовыми параметрами?

## Шаблоны с переменным числом аргументов

- Как объявить функцию-шаблон с переменным количеством аргументов?
- Как объявить класс-шаблон с переменным количеством аргументов?
- Объясните понятие *parameter pack* и *pack expansion*. Чем отличается пакет параметров шаблона от пакета параметров функции?
- Напишите шаблонную функцию Second, принимающую произвольное количество аргументов любого типа и возвращающую второй аргумент.
- В чём заключаются преимущества fold-выражений в сочетании с variadic-шаблонами?
- Как из шаблонной функции, принимающей произвольное число аргументов вызвать другую функцию
   с произвольным количеством аргументов и передать туда все аргументы?
- Свёрточные выражения. Напишите шаблонную функцию `ApplyToMany`,
   которая принимает функциональный объект f и произвольное количество аргументов,
   а потом последовательно вызывает f с каждым из переданных аргументов.
- Напишите шаблонную функцию `EqualsToOneOf`, принимающую один или более аргументов. Функция должна вернуть true,
   если ёё первый аргумент равен одному из остальных аргументов и false в противном случае.
- Напишите шаблонную функцию Last, принимающую произвольное количество аргументов любого типа
   и возвращающую свой последний аргумент.
- Опишите отличие свёрток левоассоциативных и правоассоциативных.
    Когда важно явно задавать «инициализирующее» значение в бинарной свёртке?
- Рассмотрите функцию фабрики `std::make_shared`.
    Объясните, как она использует perfect-forwarding и variadic-шаблоны для вызова произвольного конструктора.
